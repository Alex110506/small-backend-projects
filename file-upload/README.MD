# Simple File Upload Backend (Express + express-fileupload + MongoDB)

## Installation

1. **Set up environment variables**
    ```bash
   MONGO_URI=exmapleuri.com

2. **Run the project**

    ```bash
    npm install
    npm start
